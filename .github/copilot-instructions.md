# Copilot Instructions

## Project Guidelines
- For Android release publishing in this workspace, use the existing Release keystore configuration (`gotrexia-release.keystore`) and do not pass signing passwords via command-line properties.